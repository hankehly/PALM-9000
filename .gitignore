# Python
__pycache__

# Virtual environments
.venv

# Secrets and local configuration
.env
# Credential files. This replaces a previous blanket `*.json`, which also hid
# legitimate config (editor settings, tool configs). No .json file has ever
# been committed to this repo, so nothing was relying on the broad form.
*-credentials.json
*.credentials.json
credentials.json
service-account*.json

# Editor and OS cruft
.DS_Store

# Testing and coverage
.coverage
.pytest_cache
coverage.xml
htmlcov

# Audio artifacts
*.pcm
*.wav

# Models and vector stores
*.onnx
*.ppn
*.pv
chroma_db
models/*
!models/.gitkeep

# Archives
*.zip
